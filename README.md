git config --global user.email "vickyb1.kv1@gmail.com"
  git config --global 

user.name "vickyb1kv1-beep"

git init

git config --global user.email "you@example.com"
  git config --global user.name "Your Name"

git branch -M main (main branch name)

git remote add origin https://github.com/vickyb1kv1-beep/Course_1.git


git add Untitled-2.py
git push -u origin main

git pull origin main

git branch feature/new-branch
git checkout feature/test
git push -u origin feature/test 
git branch -d feature/test-delete
